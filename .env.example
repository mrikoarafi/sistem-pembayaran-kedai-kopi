# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=kedai_user
DB_PASSWORD=your_password_here
DB_NAME=kedai_kopi_db

# Database URL untuk Prisma
DATABASE_URL="postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}?schema=public"
